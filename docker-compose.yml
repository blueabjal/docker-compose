version: "3.5"
services:
  crms:
    image: crms:v32
    ports:
      - "8081:8080"
    networks:
      frontend:
  upload:
    image: upload:v10
    ports:
      - "8082:8080"
    networks:
      frontend:
networks:
  frontend:
    external: true

