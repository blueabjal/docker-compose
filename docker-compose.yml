version: "3.5"
services:
  crms:
    image: upload:v6
    ports:
      - "8081:8080"
    networks:
      frontend:
  upload:
    image: upload:v
    ports:
      - "8082:8080"
    networks:
      frontend:
networks:
  frontend:
    external: true

