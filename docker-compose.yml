version: "3.5"
services:
  crms:
    image: app:v13
    ports:
      - "80:8080"
    networks:
      frontend:
  upload:
    image: app:v2
    ports:
      - "80:8080"
    networks:
      frontend:
networks:
  frontend:
    external: true
